# .bazelrc

# Target arm-none-eabi toolchain for target builds.
build --crosstool_top=//toolchains:arm-none-eabi

# Target the default cpp compiler for host builds.
build --host_crosstool_top=@bazel_tools//tools/cpp:toolchain